# Listener インストール手順

社内の PC に Listener を入れて使えるようにするまでの手順です。
所要時間は 30分ほど（ほとんどはダウンロードの待ち時間）。

- **管理者権限は不要です。**
- Windows 10 / 11（64bit）で動きます。
- ディスクの空きが **4GB** ほど必要です。
- **ネット接続が必要なのは、この手順の間だけです。** 使い始めたあとは
  音声も文字起こしも要約も、すべて手元の PC の中だけで処理されます。
  外部に音声やテキストが送られることはありません。

---

## 手順1. 3つのファイルを取得する

ブラウザで次のページを開きます。

https://github.com/Kishi-Satoshi/Listener/releases/latest

「Assets」の中から、次の **3つ** をダウンロードしてください。

| ファイル | 用途 |
| --- | --- |
| `Listener-<版>-setup.exe` | アプリ本体（約 75MB） |
| `setup-local-engine.ps1` | 文字起こしエンジンの導入用 |
| `setup-summarizer.ps1` | 要約エンジンの導入用 |

### ダウンロードが途中で止まる場合

社内回線は大きなファイルのダウンロードを途中で切ることがあります
（115〜122MB あたりで切れる例を確認しています）。
その場合は、同じページの `get-installer.ps1` だけを落として実行してください。
切断されても続きから再開し、最後に SHA256 で壊れていないか確かめます。
上の 3ファイルが同じフォルダに揃います。

```powershell
powershell -ExecutionPolicy Bypass -File .\get-installer.ps1
```

---

## 手順2. アプリを入れる

`Listener-<版>-setup.exe` をダブルクリックするだけです。

- インストール先は `%LOCALAPPDATA%\Programs\Listener\`（自分専用）
- デスクトップとスタートメニューにショートカットができます
- 管理者権限は聞かれません

> **「WindowsによってPCが保護されました」と出たら**
> 署名を付けていないため出ます。［詳細情報］→［実行］で進めてください。

この時点で起動はしますが、**文字起こしと要約はまだ動きません。**
エンジンを入れる手順3・手順4まで進めてください。

---

## 手順3. 文字起こしエンジンを入れる（必須・約 0.6GB）

### 先に置き場所を決める

エンジンは **スクリプトを置いたフォルダの直下** に `local-engine` という名前で作られます。
`ダウンロード` フォルダで実行すると、そこに 2.5GB が置かれたままになります。
先に置き場所を作って、そこへ 2つの `.ps1` を移してから実行してください。

```powershell
mkdir "$env:LOCALAPPDATA\Listener-engine"
```

エクスプローラーのアドレス欄に `%LOCALAPPDATA%\Listener-engine` と入れれば開けます。

> **OneDrive・Dropbox などの同期フォルダには置かないでください。**
> 「ファイルオンデマンド」で中身がクラウド側にあるだけのファイル（プレースホルダ）は、
> 見た目は正常でもエンジンが読めず、起動テストが「モデルの実体がこの PC にありません」で止まります。
> `%LOCALAPPDATA%` 配下は同期の対象外なので安全です。
ここに `setup-local-engine.ps1` と `setup-summarizer.ps1` を移動します。

### 実行する

そのフォルダで PowerShell を開き（アドレス欄に `powershell` と入力して Enter）、

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-local-engine.ps1
```

whisper.cpp 本体と、日本語特化モデル kotoba-whisper（約 577MB）、
VADモデル（約 0.9MB）を取得します。切断されても自動で続きから再開します。

終わると画面に **貼り付ける2つのパス** が表示されます。これを控えてください。

---

## 手順4. 要約エンジンを入れる（議事録の自動要約に必要・約 2GB）

同じフォルダで続けて実行します。

```powershell
powershell -ExecutionPolicy Bypass -File .\setup-summarizer.ps1
```

llama.cpp 本体と Qwen2.5-3B（約 1.9GB）を取得します。
終わると、また **貼り付ける2つのパス** が表示されます。

> メモリに余裕がある PC（16GB以上）なら `-Model 7b` を付けると要約の質が上がります。
> 迷ったら付けなくて構いません。

---

## 手順5. 設定にパスを貼る

Listener を起動し、右上の歯車から［設定］を開きます。

**文字起こしエンジン（whisper.cpp・オフライン）**
- `whisper-server.exe のパス` … 手順3で表示されたもの
- `モデルファイル（ggml-*.bin）のパス` … 手順3で表示されたもの
- `VADモデル` … 空欄のままで構いません（同じフォルダから自動で見つけます）

**要約エンジン（llama.cpp・オフライン）**
- `llama-server.exe のパス` … 手順4で表示されたもの
- `モデルファイル（*.gguf）のパス` … 手順4で表示されたもの

貼り付けるかわりに、各欄の［参照…］からファイルを選んでも構いません。

それぞれの［起動テスト］を押して **OK** が出れば完了です。
設定は入力した時点で自動保存されます（保存ボタンはありません）。

> **［起動テスト］でエラーが出る場合**
> Microsoft Visual C++ 再頒布可能パッケージが入っていないことがあります。
> https://aka.ms/vs/17/release/vc_redist.x64.exe を入れてから、もう一度お試しください。

---

## 使い方（最初の1回）

- `Alt+M` … 議事録の記録を開始／終了
- `Ctrl+Shift+Space` … 音声入力

記録を始めると画面下に録音バーが出ます。Ⅱ で一時停止、赤い ■ で停止です。
停止すると文字起こしと要約が走ります。要約の各行には、その根拠になった
発言へのリンクが自動で付きます。

Web会議の音声も一緒に録りたい場合は、［設定］→［動作］の
「議事録で相手の声も録る（パソコンから出ている音）」を有効にしてください。

---

## 更新について

［設定］→［アップデート］の［更新を確認］から更新できます。
更新で落ちてくるのは数百KBの差分だけなので、
大きなファイルを落とすのは最初の1回だけです。
エンジンは入れ直す必要がありません。

> **更新が「Electron … が必要です。インストーラーで入れ直してください」で止まったら**
> その版は差分だけでは入りません（アプリ本体を新しくする必要があります）。
> 手順1のページから新しい `Listener-<版>-setup.exe` を落として、手順2と同じように
> 入れ直してください。設定・議事録・エンジンはそのまま残ります（保存先が別だからです）。

---

## 常駐メモリの目安

エンジンは必要になったときに起動し、使わない間は止めてメモリを空けます。

| 状態 | 目安 |
| --- | --- |
| 文字起こしエンジン（whisper）が常駐 | 0.8〜1.2GB |
| 要約の生成中（llama が加わる） | 上に加えて約 3GB |

- 記録も要約もしていない時間が［設定］→「使わないエンジンを止めるまで」（既定 10 分）を
  超えると、エンジンを止めます。次に使うときに起動し直します（初回だけ数十秒かかります）。
  0 にすると止めない（常駐したまま）。
- すぐに空けたいときは、タスクトレイの「エンジンを停止（メモリを解放）」を使います。
- 要約エンジンは記録の開始時ではなく、要約の直前に起動します。記録だけでは約 3GB は増えません。

---

## データの保存先とバックアップ

議事録・文字起こし・設定は次の場所にあります。

```
%APPDATA%\listener\
```

［設定］→［データ保存先を開く］から開けます。
バックアップはこのフォルダをコピーするだけです。

- `%APPDATA%`（Roaming）配下は、社内のプロファイル同期（移動ユーザープロファイル等）で
  サーバーへ複製されることがあります。議事録を同期に載せたくない場合は、［設定］→「保存先を変更…」で
  `%LOCALAPPDATA%\Listener\data` などへ移せます。移動しても元の場所のデータは消しません
  （不要になったら手で消してください）。
- OneDrive 等の同期フォルダやネットワークドライブは避けてください（記録中に書き込みが止まると、
  その区間の文字起こしが失敗します）。

---

## 困ったときは

| 症状 | 対処 |
| --- | --- |
| ［起動テスト］が失敗する | Visual C++ 再頒布可能パッケージを入れる（上記） |
| ダウンロードが途中で切れる | もう一度同じスクリプトを実行する。続きから再開します |
| 「準備しています」が長い | 初回だけモデルの読み込みに 1〜2分かかります |
| 文字起こしが遅い | ［設定］→ VADを有効にする／スレッド数を上げる |
| 設定画面のパスがわからない | 手順3・4のスクリプトをもう一度実行すると再表示されます |
