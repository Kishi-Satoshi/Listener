# 変更履歴

各版の見出し（`## v0.8.0`）の下に書いた内容が、そのままリリース本文になり、
アプリの [設定] → [アップデート] に表示されます。
リリースワークフローがこのファイルから該当箇所を抜き出します。

## v0.9.7

文字起こしが途中で失われる問題を根本から直しました。画面の使い勝手も改善しています。

- 録音の区切りが遅れて1区間が異常に長くなり、文字起こしが時間切れで
  丸ごと失われることがあった問題を、三重に対策しました。
  録音ウィンドウが他の窓に隠れてもタイマーを間引かせない・
  区切りの見張りを音声処理側にも置く・文字起こしの待ち時間を音声の長さに
  比例させる（従来は固定4分で、長い区間が必ず失われていました）。
  あわせて、録音や文字起こしの最中はWindowsの省電力でアプリが
  眠らされないようにしました。
- 要約の文体を報告文書の常体（「〜を実施」「〜は完了」）にしました。
  これまでは「です・ます」調で、そのまま報告に使うには直しが必要でした。
- 画面の配色を選べるようにしました（Windowsの設定に合わせる／ライト／ダーク）。
- 設定のオン・オフをトグルスイッチにしました。
- 議事録一覧を1件2行に揃えて、たくさん見えるようにしました。
  未完了タスクの表示は一覧からは外しています（アクションタブで見られます）。
- 「コピー」は開いているタブの中身を写すようにしました。議事録タブなら議事録、
  文字起こしタブなら文字起こし、メモタブならメモです。
- 「全文検索」を「文字起こし検索」にしました。検索対象は文字起こしだけです。
  発言したかどうかを探す用途では、編集されうる要約より原文の方が確かです。
- 「会議タイプ」の名称を「要約タイプ」に変えました。
- 並べ替えのつまみが環境によって3点に見えていたのを直しました。

## v0.9.6

使い勝手の改善と、文字起こしの言葉の混入を直しました。

- 記録を新しく始めたとき、前の議事録が開いたままにならず、
  白紙の記録画面から始まるようにしました。
- 会議の所要時間を「開始から停止まで」で記録するようにしました。
  これまでは停止後の文字起こし待ちまで含まれてしまい、
  4分の会議が12分と記録されることがありました。画面の時計も停止で止まります。
- 議事録の行を、左端のつまみ（⋮⋮）のドラッグで並べ替えられるようにしました。
- 会議タイプの初期値を「定例・進捗報告」にしました。タイトルなどに
  手がかりが無い会議は、これまで「一般・その他」になっていました。
- 会議タイプを選ぶと、そのタイプに合ったアジェンダの雛形がメモ欄に入ります。
  自分で書いたメモがあるときは上書きしません。
- ウィンドウを閉じたときの動きを選べるようにしました。初期値は
  「閉じたらアプリも終了」です。これまでのようにタスクトレイへ残したい場合は
  設定で切り替えられます。記録中に閉じたときは確認が出ます。
- Markdown保存ボタンを外しました（「コピー」はこれまで通り使えます）。
- 文字起こしへ渡していた内部の文言が、まれに本文へ混ざる問題を直しました
  （「不具合の報告」が「句読報告」になる等）。

## v0.9.5

Web会議の相手の声を議事録に残せるようにしました。要約の書式のゆれも直しています。

- [設定] → [議事録で相手の声も録る] を有効にすると、マイクに加えて
  パソコンから出ている音も一緒に記録します。これまではマイクだけだったため、
  ヘッドホンを使っていると相手の発言が一切残らず、リモート会議の議事録として
  成立しませんでした。Windows でのみ動作し、初期値はオフです。
  取り込めなかったときはマイクだけで記録を続け、録音バーにそう表示します。
  ※ 録音していることは会議の参加者へ伝えてください。
- 要約に「**受注管理の改修**」のようなアスタリスクがそのまま出ていたのを直しました。
  見た目だけの問題ではなく、この記号のせいで出典リンクが付かなくなる要点が
  ありました。
- 出典の印を、発言の時刻（例: 1:15）にしました。これまでは常に「1」と出ており、
  要約のすべての要点が同じ場所を指していても気づけませんでした。
  文字起こしの表示と同じ時刻なので、そのまま見比べられます。
- 「アクションアイテム」に中身があるのに「特になし」も並ぶことがあったのを
  直しました。本当に何も無い場合の「特になし」はこれまでどおり残ります。
- 会議タイプを手で選んだときも、自動判定が何と答えたかを併記するようにしました。
- 文字起こしに業務でよく使う言葉のヒントを渡すようにしました
  （「不具合」「改修」「結合テスト」など）。認識言語が日本語のときだけ使われ、
  ユーザー辞書がある場合はそちらが優先されます。ユーザー辞書のヒントが
  録音の最初の30秒にしか効いていなかったのも直しています。

## v0.9.4

音が入っていないまま録音が進むのを防ぎます。

- 録音中に8秒以上まったく音が入らない場合、録音バーに
  「音が入っていません。マイクを確認してください」と表示します。
  これまではエラーも出ないまま最後まで録り続け、文字起こしが空になって
  初めて気づく状態でした。マイクが Windows 側で塞がれている場合、
  拒否ではなく無音のまま録れてしまうことがあります。
- 音が入り始めれば表示は元に戻ります。録音そのものは止めません。

## v0.9.3

記録中のメモと、会議タイプの表示を直しました。

- 記録中にメモを書けるようにしました。ここに議題などを書いておくと、
  そのまま要約のヒントとして使われます。これまでは受け口だけがあって
  画面から書く手段が無く、記録中のメモは常に空のままでした。
- 議事録を選ぶ前に、中身の無い「会議タイプ」の選択欄が出ていたのをやめました。
  タイプは議事録ごとの設定なので、議事録を開いてから選びます。

## v0.9.2

設定が消えることがある問題を直しました。

- 設定と履歴の保存を、一時ファイルに書いてから置き換える方式にしました。
  これまでは直接書き込んでいたため、書き込みの途中でアプリが落ちると
  壊れたファイルが残り、次回起動時に既定値へ戻ってエンジンのパスなどが
  失われることがありました（議事録は元から安全な方式でした）。
- 設定ファイルが壊れていた場合は、既定値に戻す前に
  `settings.json.broken-<日時>` として退避します。中身を取り出せます。
- データ保存先の記載を実態に合わせました。正しくは `%APPDATA%\listener`
  です（README は `%APPDATA%\koetype` と案内していましたが、そこは
  現在使われておらず、バックアップ手順としても誤りでした）。

## v0.9.1

タスクトレイのアイコンが透明になっていたのを直しました。

- トレイのアイコンを SVG から作っていましたが、Electron の画像機能は
  SVG を扱えず（PNG / JPEG、Windows では ICO のみ）、常に空の画像に
  なっていました。実際のアイコンファイルを同梱して読み込むようにしています。
- 画面の拡大率に合わせて 16〜64px から適切なサイズが選ばれます。
- 録音中は色と図柄が変わるので、トレイを見れば記録中かどうかが分かります。

## v0.9.0

インストーラー版でもアプリ内更新が使えるようになりました。

- `Listener.exe` としてインストールできるようになりました。管理者権限は不要で、
  デスクトップとスタートメニューに登録され、「サインイン時に自動起動」も使えます。
- これまでインストーラー版はアプリ内更新ができず、更新のたびに80MB超の
  インストーラーを落とし直す必要がありました。アプリの中身を書庫に固めるのを
  やめたため、インストーラー版でも数百KBの更新がそのまま適用できます。
- 更新できるかどうかを「インストーラー版かどうか」ではなく
  「実際に差し替えられるか」で判断するようにしました。書き込めない場所に
  入っている場合は、その旨を表示して案内に切り替えます。

- インストーラーが途中で落ちてくる回線向けに `get-installer.ps1` を添付しました。
  切断されても続きから再開し、最後にサイズと SHA256 を照合します。

※ ポータブル版（`npm run dist`）は起動のたびに一時フォルダへ展開して動くため、
アプリ内更新は次回起動に残りません。自動更新を使う場合はインストーラー版を配布してください。

## v0.8.2

社内プロキシ環境で「更新を確認」が必ず失敗する問題を直しました。

- 更新の通信を Electron のネットワーク層に通すようにしました。
  Windows のプロキシ設定・PAC・資格情報・証明書ストアがそのまま効くため、
  SSL検査のある社内プロキシ配下でも更新を確認できます。
  （これまでは Node の通信を使っており、プロキシ設定も証明書ストアも
  参照しないため、接続できず「ネットワークに接続できません」になっていました）
- 失敗した理由を update.log に残すようにしました。切断・プロキシ・証明書の
  どれで止まっているかを切り分けられます。

## v0.8.1

更新に気づける導線を足しました。

- 更新があるとき、画面の左下に告知が出るようになりました。
  押すか閉じるまで残るので、これまでのように数秒で消えるトーストを
  見逃して更新に気づけない、ということがなくなります。
- 告知から「更新して再起動」まで、その場で完結します。変更点も開けます。
- インストーラー版では「ダウンロード」に切り替わり、配布ページを開きます。

## v0.8.0

初回リリース。

- 完全オフラインの音声入力・議事録ツール（文字起こし: whisper.cpp / 要約: llama.cpp）
- 要約の各要点に、根拠となった発言への出典リンクが自動で付きます
- 会議タイプ別の要約テンプレート（定例／部会／1on1／商談／面接／ブレスト）
- アクションアイテムの担当者・期限を自動抽出し、実日付へ正規化

更新はアプリ本体（約180KB）のみを取得します。
エンジン・モデル・保存済みの議事録はそのままです。
適用前に現行の `src/` を `src.backup-*` として退避します（1世代だけ残ります）。
